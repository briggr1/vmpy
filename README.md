vmpy
====

esxi vm guest provisioning and control

Uses psphere to invoke the VMWare SDK
-create/revert/delete snapshots
-power on/off/suspend guests
-customize linux guest hostname, dns, ipv4 settings
